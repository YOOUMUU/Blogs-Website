<%- include("partials/header") -%>

    <h1>🛀 浴室沉思</h1>
    <p><%=startingContent%></p>

    <% newPost.forEach( item => { %>
        <div class="mt-5">
            <h2><%= item.title %></h2>
            <p><%= `${item.content.substring(0,100)} ...` %>
            <a href="blogs/<%=item._id%>">更多</a></p>
        </div>
    <% }); %>
    <div class="mt-5 mb-5">
        <a href="/compose"><button class=" btn btn-lg btn-primary">来写点什么</button></a>
    </div>
<%- include("partials/footer") -%>